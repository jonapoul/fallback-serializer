---
comments:
  active: false

complexity:
  active: false

naming:
  FunctionNameMaxLength:
    maximumFunctionNameLength: 50

style:
  MultilineRawStringIndentation:
    # ktfmt owns the indentation
    active: false
  RedundantVisibilityModifier:
    # explicit API mode needs the public modifiers
    active: false
